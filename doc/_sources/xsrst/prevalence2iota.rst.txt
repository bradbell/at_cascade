!!!!!!!!!!!!!!!
prevalence2iota
!!!!!!!!!!!!!!!

.. toctree::
   :maxdepth: 1
   :hidden:

   prevalence2iota_py

.. meta::
   :keywords: prevalence2iota, example, estimation, iota, from, prevalence, data

.. index:: prevalence2iota, example, estimation, iota, from, prevalence, data

.. _prevalence2iota:

Example Estimation of iota From Prevalence Data
###############################################
.. contents::
   :local:

.. meta::
   :keywords: nodes

.. index:: nodes

.. _prevalence2iota.nodes:

Nodes
*****
The following is a diagram of the node tree for this example::

                n0
          /-----/\-----\
        n1              n2
       /  \            /  \
     n3    n4        n5    n6

For this example the :ref:`glossary.root_node` is n0 and
{ n3, n4, n5, n6 } is the :ref:`glossary.leaf_node_set`.

.. meta::
   :keywords: rates

.. index:: rates

.. _prevalence2iota.rates:

Rates
*****
The only non-zero dismod_at rates for this example are
:ref:`glossary.iota` and :ref:`glossary.omega`.
We use :math:`\iota(a, n, I)` and *iota_n* to denote the value for *iota*
as a function of age *a*, node number *n*, and income *I*.
We use :math:`\omega(a, n)` and *iota_n* to denote the value for *omega*
as a function of age *a* and node number *n*.

.. meta::
   :keywords: covariate

.. index:: covariate

.. _prevalence2iota.covariate:

Covariate
*********
There are two covariates for this example is *income* and *one*.
The reference value for *income* is the average income corresponding
to the :ref:`glossary.fit_node`.
The *one* covariate is always equal to 1 and its reference is always zero.

.. meta::
   :keywords: r_n

.. index:: r_n

.. _prevalence2iota.covariate.r_n:

r_n
===
We use *r_n* for the reference value of *income* at node *n*.
The code below sets this reference using the name avg_income:

.. literalinclude:: ../../example/prevalence2iota.py
    :lines: 281-284
    :language: py

.. meta::
   :keywords: alpha

.. index:: alpha

.. _prevalence2iota.covariate.alpha:

alpha
=====
We use *alpha* and :math:`\alpha`
for the :ref:`glossary.rate_value` covariate multiplier
which multiplies *income*.
This multiplier affects the value of *iota*.
The true value for *alpha* (used which simulating the data) is

.. literalinclude:: ../../example/prevalence2iota.py
    :lines: 277-277
    :language: py

.. meta::
   :keywords: gamma

.. index:: gamma

.. _prevalence2iota.covariate.gamma:

gamma
=====
We use *gamma*
for the :ref:`glossary.meas_noise` covariate multiplier
which multiplies *one*.
This multiplier add to the nose level for prevalence in log space,
because the density for the prevalence data is log Gaussian.

.. meta::
   :keywords: random, effects

.. index:: random, effects

.. _prevalence2iota.random_effects:

Random Effects
**************
For each node, there is a random effect on *iota* that is constant
in age and time. Note that the leaf nodes have random effect for the node
above them as well as their own random effect.

.. meta::
   :keywords: s_n

.. index:: s_n

.. _prevalence2iota.random_effects.s_n:

s_n
===
We use *s_n* to denote the sum of the random effects for node *n*.
The code below sets this sum using the name sum_random:

.. literalinclude:: ../../example/prevalence2iota.py
    :lines: 288-294
    :language: py

.. meta::
   :keywords: simulated, data

.. index:: simulated, data

.. _prevalence2iota.simulated_data:

Simulated Data
**************
For this example everything is constant in time so the
functions below do not depend on time.

.. meta::
   :keywords: random, seed

.. index:: random, seed

.. _prevalence2iota.simulated_data.random_seed:

Random Seed
===========
The random seed can be used to reproduce results.
If the original value of this setting is zero, the clock is used get
a random seed. The actual value or *random_seed* is always printed.

.. literalinclude:: ../../example/prevalence2iota.py
    :lines: 268-273
    :language: py

.. meta::
   :keywords: iota_true(a, , n, , i)

.. index:: iota_true(a, , n, , i)

.. _prevalence2iota.simulated_data.iota_true(a,_n,_i):

iota_true(a, n, I)
==================
This is the true value for *iota* in node *n* at age *a* and income *I*:

.. literalinclude:: ../../example/prevalence2iota.py
    :lines: 319-322
    :language: py

.. meta::
   :keywords: omega_true(a, , n)

.. index:: omega_true(a, , n)

.. _prevalence2iota.simulated_data.omega_true(a,_n):

omega_true(a, n)
================
This is the true value for *omega* in node *n* and age *a*:

.. literalinclude:: ../../example/prevalence2iota.py
    :lines: 326-329
    :language: py

.. meta::
   :keywords: y_i

.. index:: y_i

.. _prevalence2iota.simulated_data.y_i:

y_i
===
The only simulated integrand for this example is :ref:`glossary.prevalence`.
This data is simulated without any noise; i.e.,
the i-th measurement is simulated as
*y_i = iota_true(a_i, n_i, I_i)*
where *a_i* is the age,
*n_i* is the node,
and *I_i* is the income for the i-th measurement.
The data is modeled as having noise even though there is no simulated noise.

.. meta::
   :keywords: n_i

.. index:: n_i

.. _prevalence2iota.simulated_data.n_i:

n_i
===
Data is only simulated for the leaf nodes; i.e.,
each *n_i* is in the set { n3, n4, n5, n6 }.
Since the data does not have any nose, the data residuals are a measure
of how good the fit is for the leaf nodes.

.. meta::
   :keywords: a_i

.. index:: a_i

.. _prevalence2iota.simulated_data.a_i:

a_i
===
For each leaf node, data is generated on the following *age_grid*:

.. literalinclude:: ../../example/prevalence2iota.py
    :lines: 298-298
    :language: py

.. meta::
   :keywords: i_i

.. index:: i_i

.. _prevalence2iota.simulated_data.i_i:

I_i
===
For each leaf node and each age in *age_grid*,
data is generated for the following *income_grid*:

.. literalinclude:: ../../example/prevalence2iota.py
    :lines: 302-313
    :language: py

Note that the check of the fit for the leaf nodes expects much more accuracy
when the income grid is not chosen randomly.

.. meta::
   :keywords: parent, smoothing

.. index:: parent, smoothing

.. _prevalence2iota.parent_smoothing:

Parent Smoothing
****************

.. meta::
   :keywords: omega

.. index:: omega

.. _prevalence2iota.parent_smoothing.omega:

omega
=====
The parent smoothing constrains *omega* to be equal to
:math:`\omega(a, n)` where  *a* is each value in the age grid and
*n* is the current node.

.. meta::
   :keywords: iota

.. index:: iota

.. _prevalence2iota.parent_smoothing.iota:

iota
====
This is the smoothing used in the *fit_node* model for *iota*.
(The :ref:`glossary.fit_node` is the parent node in dismod_at notation.)
This smoothing uses the *age_gird* and one time point.
There are no dtime priors because there is only one time point.

.. meta::
   :keywords: value, prior

.. index:: value, prior

.. _prevalence2iota.parent_smoothing.iota.value_prior:

Value Prior
-----------
The *fit_node* value prior is uniform with lower limit
*iota_true(0)/10* and upper limit *iota_true(100)\*10*.
The mean is *iota_true(50)*
(which is used to initialize the optimization.)

.. meta::
   :keywords: dage, prior

.. index:: dage, prior

.. _prevalence2iota.parent_smoothing.iota.dage_prior:

Dage Prior
-----------
The prior for age differences is log Gaussian with mean zero,
standard deviation 1.0, and :ref:`glossary.eta` equal to
*iota_true(0)/1000* .

.. meta::
   :keywords: child, smoothing

.. index:: child, smoothing

.. _prevalence2iota.child_smoothing:

Child Smoothing
***************

.. meta::
   :keywords: omega

.. index:: omega

.. _prevalence2iota.child_smoothing.omega:

omega
=====
The child smoothing constrains *omega* to be equal to
:math:`\omega(a, n)` where  *a* is each value in the age grid and
*n* is the current node.

.. meta::
   :keywords: iota

.. index:: iota

.. _prevalence2iota.child_smoothing.iota:

iota
====
This is the smoothing used in the model for the *iota*
random effect for each child of the *fit_node*.
The smoothing only has one age and one time point; i.e.,
the corresponding function is constant in age and time.
There are no dage or dtime priors because there is only one
age and one time point.

.. meta::
   :keywords: value, prior

.. index:: value, prior

.. _prevalence2iota.child_smoothing.iota.value_prior:

Value Prior
-----------
This value prior is gaussian with mean zero and standard deviation 1.0.
There are no upper or lower limits in this prior.

.. meta::
   :keywords: alpha, smoothing

.. index:: alpha, smoothing

.. _prevalence2iota.alpha_smoothing:

Alpha Smoothing
***************
This is the smoothing used in the model for *alpha*.
There are no dage or dtime priors because there is only one
age and one time point in this smoothing.

.. meta::
   :keywords: value, prior

.. index:: value, prior

.. _prevalence2iota.alpha_smoothing.value_prior:

Value Prior
===========
This value prior is uniform with lower limit *-10\*|alpha_true|*,
upper limit *+10\*|alpha_true|* and mean zero.
(The mean is used to initialize the optimization.)

.. csv-table::
    :header: "Child", "Title"
    :widths: 20, 80

    "prevalence2iota_py", :ref:`prevalence2iota_py`

----

xsrst input file: ``example/prevalence2iota.py``

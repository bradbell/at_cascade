.. include:: ../preamble.rst

!!!!!!!!!!!!
iota_and_chi
!!!!!!!!!!!!

.. toctree::
   :maxdepth: 1
   :hidden:

   iota_and_chi_py

.. meta::
   :keywords: iota_and_chi, example, estimation, iota, chi

.. index:: iota_and_chi, example, estimation, iota, chi

.. _iota_and_chi:

Example Estimation of iota and chi
##################################
.. contents::
   :local:

For this example everything is constant in time so the
functions below do not depend on time.

.. meta::
   :keywords: under, construction

.. index:: under, construction

.. _iota_and_chi.under_construction:

Under Construction
******************
This example is not yet working.

.. meta::
   :keywords: nodes

.. index:: nodes

.. _iota_and_chi.nodes:

Nodes
*****
The following is a diagram of the node tree for this example.
The :ref:`glossary.root_node` is n0,
the :ref:`glossary.fit_goal_set` and the leaf node set
are both {n3, n4, n5, n6} for this example::

                n0
          /-----/\-----\
        n1              n2
       /  \            /  \
    (n3)  (n4)      (n5)  (n6)

.. meta::
   :keywords: fit_goal_set

.. index:: fit_goal_set

.. _iota_and_chi.nodes.fit_goal_set:

fit_goal_set
============

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 306-306
    :language: py

.. meta::
   :keywords: rates

.. index:: rates

.. _iota_and_chi.rates:

Rates
*****
The non-zero dismod_at rates for this example are
:ref:`glossary.iota`, :ref:`glossary.chi`, and :ref:`glossary.omega`.
We use *iota(a, n, I)* , *chi(a, n, I)*
to denote the value for iota and chi
as a function of age *a*, node number *n*, and income *I*.
We use *omega(a, n)* and *omega_n* to denote the value for omega
as a function of age *a* and node number *n*.

.. meta::
   :keywords: covariate

.. index:: covariate

.. _iota_and_chi.covariate:

Covariate
*********
There are two covariates for this example is income and *one*.
The reference value for income is the average income corresponding
to the :ref:`glossary.fit_node`.
The *one* covariate is always equal to 1 and its reference is always zero.

.. meta::
   :keywords: r_n

.. index:: r_n

.. _iota_and_chi.covariate.r_n:

r_n
===
We use *r_n* for the reference value of income at node *n*.
The code below sets this reference using the name avg_income:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 322-325
    :language: py

.. meta::
   :keywords: alpha

.. index:: alpha

.. _iota_and_chi.covariate.alpha:

alpha
=====
We use *alpha*
for the :ref:`glossary.rate_value` covariate multiplier
which multiplies income.
This multiplier affects the value of iota.
The true value for *alpha* (used which simulating the data) is

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 318-318
    :language: py

.. meta::
   :keywords: gamma

.. index:: gamma

.. _iota_and_chi.covariate.gamma:

gamma
=====
We use *gamma_prevalence* ( *gamma_mtexcess* )
for the :ref:`glossary.meas_noise` covariate multiplier
which multiplies *one* add affects prevalence noise ( mtexcess noise) .
These multipliers adds to the nose level in log space,
because the corresponding densities are log Gaussian.

.. meta::
   :keywords: random, effects

.. index:: random, effects

.. _iota_and_chi.random_effects:

Random Effects
**************
For each node, there is a random effect on iota and chi that is constant
in age and time. Note that the leaf nodes have random effect for the node
above them as well as their own random effect.

.. meta::
   :keywords: s_n

.. index:: s_n

.. _iota_and_chi.random_effects.s_n:

s_n
===
We use *s_n* to denote the sum of the random effects for node *n*.
The code below sets this sum using the name sum_random:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 329-335
    :language: py

.. meta::
   :keywords: simulated, data

.. index:: simulated, data

.. _iota_and_chi.simulated_data:

Simulated Data
**************

.. meta::
   :keywords: random, seed

.. index:: random, seed

.. _iota_and_chi.simulated_data.random_seed:

Random Seed
===========
The random seed can be used to reproduce results.
If the original value of this setting is zero, the clock is used get
a random seed. The actual value or *random_seed* is always printed.

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 310-314
    :language: py

.. meta::
   :keywords: rate_true(rate, , a, , n, , i)

.. index:: rate_true(rate, , a, , n, , i)

.. _iota_and_chi.simulated_data.rate_true(rate,_a,_n,_i):

rate_true(rate, a, n, I)
========================
For *rate* equal to iota, chi, and omega,
this is the true value for *rate* in node *n* at age *a* and income *I*:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 361-372
    :language: py

.. meta::
   :keywords: y_i

.. index:: y_i

.. _iota_and_chi.simulated_data.y_i:

y_i
===
The simulated integrands for this example are
:ref:`glossary.prevalence` and :ref:`glossary.mtspecific`.
The data is simulated without any noise
but it is modeled as having noise.

.. meta::
   :keywords: n_i

.. index:: n_i

.. _iota_and_chi.simulated_data.n_i:

n_i
===
Data is only simulated for the leaf nodes; i.e.,
each *n_i* is in the set { n3, n4, n5, n6 }.
Since the data does not have any nose, the data residuals are a measure
of how good the fit is for the nodes in the fit_goal_set.

.. meta::
   :keywords: a_i

.. index:: a_i

.. _iota_and_chi.simulated_data.a_i:

a_i
===
For each leaf node, data is generated on the following *age_grid*:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 339-339
    :language: py

.. meta::
   :keywords: i_i

.. index:: i_i

.. _iota_and_chi.simulated_data.i_i:

I_i
===
For each leaf node and each age in *age_grid*,
data is generated for the following *income_grid*:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 343-355
    :language: py

Note that the check of the fit for the nodes in the fit_goal_set
expects much more accuracy when the income grid is not chosen randomly.

.. meta::
   :keywords: omega, constraints

.. index:: omega, constraints

.. _iota_and_chi.omega_constraints:

Omega Constraints
*****************
The :ref:`omega_constraint<omega_constraint>` routine is used
to set the value of omega in the parent and child nodes.

.. meta::
   :keywords: parent, rate, smoothing

.. index:: parent, rate, smoothing

.. _iota_and_chi.parent_rate_smoothing:

Parent Rate Smoothing
*********************
The parent smoothings use the true value of iota and chi at age 50
and in the root_node:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 392-393
    :language: py

.. meta::
   :keywords: iota, chi

.. index:: iota, chi

.. _iota_and_chi.parent_rate_smoothing.iota_and_chi:

iota and chi
============
This is the smoothing used in the fit_node model for the rates.
Note that the value part of this smoothing is only used for the *root_node*.
This smoothing uses the *age_gird* and one time point.
There are no :ref:`glossary.dtime` priors because there is only one time point.

.. meta::
   :keywords: value, prior

.. index:: value, prior

.. _iota_and_chi.parent_rate_smoothing.value_prior:

Value Prior
===========
The following is the value prior used for the fit_node

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 401-408
    :language: py


.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 413-420
    :language: py

The mean and standard deviation are only used for the root_node.
The :ref:`create_child_node_db<create_child_node_db>`
routine replaces them for other nodes.

.. meta::
   :keywords: dage, prior

.. index:: dage, prior

.. _iota_and_chi.parent_rate_smoothing.dage_prior:

dage Prior
==========
The following is the dage prior used for the fit_node:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 426-431
    :language: py

.. meta::
   :keywords: child, rate, smoothing

.. index:: child, rate, smoothing

.. _iota_and_chi.child_rate_smoothing:

Child Rate Smoothing
********************
This is the smoothing used for the
random effect for each child of the fit_node.
There are no :ref:`glossary.dage` or dtime
priors because there is only one age and one time point in this smoothing.

.. meta::
   :keywords: value, prior

.. index:: value, prior

.. _iota_and_chi.child_rate_smoothing.value_prior:

Value Prior
===========
The following is the value prior used for the children of the fit_node:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 436-440
    :language: py

.. meta::
   :keywords: alpha, smoothing

.. index:: alpha, smoothing

.. _iota_and_chi.alpha_smoothing:

Alpha Smoothing
***************
This is the smoothing used for *alpha* which multiplies the income covariate.
There is only one age and one time point in this smoothing
so it does not have dage or dtime priors.

.. meta::
   :keywords: value, prior

.. index:: value, prior

.. _iota_and_chi.alpha_smoothing.value_prior:

Value Prior
===========
The following is the value prior used for this smoothing:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 445-451
    :language: py

The mean and standard deviation are only used for the root_node.
The create_child_node_db
routine replaces them for other nodes.

.. meta::
   :keywords: gamma, smoothing

.. index:: gamma, smoothing

.. _iota_and_chi.gamma_smoothing:

Gamma Smoothing
***************
This is the smoothing used for *gamma_Sincidence* ( *gamma_mtexcess)
which multiplies the *one* covariate and adds noise to the
model for the Sincidence ( mtexcess) measurements.
There is only one age and one time point in this smoothing
so it does not have dage or dtime priors.
In addition, the value prior has upper and lower limits equal
to the constant returned by the lambda function in this smoothing:

.. literalinclude:: ../../example/iota_and_chi.py
    :lines: 495-501
    :language: py

.. meta::
   :keywords: checking, the, fit

.. index:: checking, the, fit

.. _iota_and_chi.checking_the_fit:

Checking The Fit
****************
The results of the fit are in the
:ref:`cascade_fit_node.dismod_db.predict` and
:ref:`cascade_fit_node.dismod_db.c_predict_fit_var`
tables of the fit_node_database corresponding to each node.
The ``check_fit`` routine uses these tables to check that fit
against the truth.

.. csv-table::
    :header: "Child", "Title"
    :widths: 20, 80

    "iota_and_chi_py", :ref:`iota_and_chi_py`

----

xsrst input file: ``example/iota_and_chi.py``

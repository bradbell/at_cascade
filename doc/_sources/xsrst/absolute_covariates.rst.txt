.. include:: ../preamble.rst

!!!!!!!!!!!!!!!!!!!
absolute_covariates
!!!!!!!!!!!!!!!!!!!

.. toctree::
   :maxdepth: 1
   :hidden:

   absolute_covariates_py

.. meta::
   :keywords: absolute_covariates, example, using, absolute_covariates, option, in, all_node_database

.. index:: absolute_covariates, example, using, absolute_covariates, option, in, all_node_database

.. _absolute_covariates:

Example Using absolute_covariates Option in all_node_database
#############################################################
.. contents::
   :local:

For this example everything is constant w.r.t. age and time.

.. meta::
   :keywords: nodes

.. index:: nodes

.. _absolute_covariates.nodes:

Nodes
*****
The following is a diagram of the node tree for this example.
The :ref:`glossary.root_node` is n0,
the :ref:`glossary.fit_goal_set` is {n3, n4, n2},
and the leaf nodes are {n3, n4, n5, n6}::

                n0
          /-----/\-----\
        n1             (n2)
       /  \            /  \
    (n3)  (n4)       n5    n6

.. meta::
   :keywords: fit_goal_set

.. index:: fit_goal_set

.. _absolute_covariates.nodes.fit_goal_set:

fit_goal_set
============

.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 214-214
    :language: py

.. meta::
   :keywords: rates

.. index:: rates

.. _absolute_covariates.rates:

Rates
*****
The only non-zero dismod_at rates for this example are
:ref:`glossary.iota`.and :ref:`glossary.omega`.

.. meta::
   :keywords: splitting, covariate

.. index:: splitting, covariate

.. _absolute_covariates.rates.splitting_covariate:

Splitting Covariate
===================
This cascade is set up to split by sex reference value; see
:ref:`split_reference_table`

.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 218-228
    :language: py

.. meta::
   :keywords: covariate

.. index:: covariate

.. _absolute_covariates.covariate:

Covariate
*********
There are three covariates for this example, sex, vaccine, and income.
Income is the only :ref:`glossary.relative_covariate`.

.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 236-255
    :language: py


.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 263-265
    :language: py

.. meta::
   :keywords: absolute_covariates

.. index:: absolute_covariates

.. _absolute_covariates.covariate.absolute_covariates:

absolute_covariates
===================
The only absolute covariate in this example is vaccine
(0 for no vaccine, 1 for yes vaccine).

.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 269-269
    :language: py

.. meta::
   :keywords: alpha

.. index:: alpha

.. _absolute_covariates.covariate.alpha:

alpha
=====
We use *alpha*\ ``[income]``
and  *alpha*\ ``[vaccine]``
for the :ref:`glossary.rate_value` covariate multipliers
that multiply the income and vaccine covariates.
The true value for *alpha* (used which simulating the data) is

.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 259-259
    :language: py

.. meta::
   :keywords: random, effects

.. index:: random, effects

.. _absolute_covariates.random_effects:

Random Effects
**************
There are no random effect for this example.

.. meta::
   :keywords: simulated, data

.. index:: simulated, data

.. _absolute_covariates.simulated_data:

Simulated Data
**************

.. meta::
   :keywords: mtall

.. index:: mtall

.. _absolute_covariates.simulated_data.mtall:

mtall
=====
The ref:`all_mtall` data fro this example is simulated as equal to
the true value for omega (see rate_true directly below) and there is
no :ref:`all_mtspecific` data for this example.

.. meta::
   :keywords: rate_true(rate, , a, , t, , n, , c)

.. index:: rate_true(rate, , a, , t, , n, , c)

.. _absolute_covariates.simulated_data.rate_true(rate,_a,_t,_n,_c):

rate_true(rate, a, t, n, c)
===========================
For *rate* equal to iota or omega,
this is the true value for *rate*
in node *n* at age *a*, time *t*, and covariate values *c=[sex,income]*.
The covariate values are a list in the
same order as the covariate table.
The values *a*, *t*, *n*, *sex*
are not used by this function for this example.

.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 275-286
    :language: py

.. meta::
   :keywords: y_i

.. index:: y_i

.. _absolute_covariates.simulated_data.y_i:

y_i
===
The only simulated integrand for this example is :ref:`glossary.sincidence`
which is a direct measurement of iota.
This data is simulated without any noise; i.e.,
the i-th measurement is simulated as
*y_i = rate_true('iota', None, None, None, [None, I_i])*
where *I_i* is the income for the i-th measurement.
The data is modeled as having noise even though there is no simulated noise.

.. meta::
   :keywords: n_i

.. index:: n_i

.. _absolute_covariates.simulated_data.n_i:

n_i
===
Data is only simulated for the leaf nodes; i.e.,
each *n_i* is in the set { n3, n4, n5, n6 }.
Since the data does not have any nose, the data residuals are a measure
of how good the fit is for the nodes in the fit_goal_set.

.. meta::
   :keywords: parent, rate, smoothing

.. index:: parent, rate, smoothing

.. _absolute_covariates.parent_rate_smoothing:

Parent Rate Smoothing
*********************
This is the iota smoothing used for the fit_node.
There are no :ref:`glossary.dage` or :ref:`glossary.dtime`
priors because there is only one age and one time point in the smoothing grid.

.. meta::
   :keywords: value, prior

.. index:: value, prior

.. _absolute_covariates.parent_rate_smoothing.value_prior:

Value Prior
===========
The following is the value prior used for the root_node

.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 303-310
    :language: py

The mean and standard deviation are only used for the root_node.
The :ref:`create_shift_db<create_shift_db>`
routine replaces them for other nodes.

.. meta::
   :keywords: alpha, smoothing

.. index:: alpha, smoothing

.. _absolute_covariates.alpha_smoothing:

Alpha Smoothing
***************
This is the smoothing used for *alpha* which multiplies the income covariate.
There is only one age and one time point in this smoothing
so it does not have dage or dtime priors.

.. meta::
   :keywords: value, prior

.. index:: value, prior

.. _absolute_covariates.alpha_smoothing.value_prior:

Value Prior
===========
The following is the value prior used for this smoothing:

.. literalinclude:: ../../example/absolute_covariates.py
    :lines: 313-327
    :language: py

The mean and standard deviation are only used for the root_node.
The create_shift_db
routine replaces them for other nodes.

.. meta::
   :keywords: checking, the, fit

.. index:: checking, the, fit

.. _absolute_covariates.checking_the_fit:

Checking The Fit
****************
The results of the fit are checked by check_cascade_node
using the :ref:`check_cascade_node.avgint_table`
that was created by the root_node_db routine.
The node_id for each row is replaced by the node_id for the
fit being checked.

.. csv-table::
    :header: "Child", "Title"
    :widths: 20, 80

    "absolute_covariates_py", :ref:`absolute_covariates_py`

----

xsrst input file: ``example/absolute_covariates.py``
